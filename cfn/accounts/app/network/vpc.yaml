AWSTemplateFormatVersion: '2010-09-09'

Description: Simple VPC

Resources:

  VPC:
    Type: AWS::EC2::VPC
    Properties:
      CidrBlock: 10.10.0.0/16

      Tags:
        - Key: Name
          Value: vesta-dev-vpc

Outputs:

  VpcId:
    Value: !Ref VPC